# Archive

Material that is **not** the active extension tree at the repository root (see the repository [`README.md`](../README.md)).

## Contents

| Path | Description |
|------|-------------|
| [`legacy/`](legacy/README.md) | **GTK 4 / libadwaita** prototype (`gtk-application/`). |
| `screen-annotations-v3-2026-04-12.zip` | Packaged extension snapshot (same family as `make pack`). |

Extension-specific notes moved to [`docs/extension/`](../docs/extension/README.md).

Install a zip with **Extensions** / `gnome-extensions install`.
